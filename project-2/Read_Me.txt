in order to run the code:

# navigate to source code
cd /path/to/project-2/source_code

# create new pytohn3 virtula environment
python3 -m venv ml-env

# activate the newly created python3 virtual environment
source ml-env/bin/activate

# install all dependencies for the code that are found in requirements.txt file
python3 -m pip install -r requirements.txt

# run the code
python3 project-2.py
